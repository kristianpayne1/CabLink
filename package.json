{
    "name": "route",
    "description": "Cablink",
    "engines": {
        "node": "10.15.0",
        "npm": "6.7.0"
    },
    "scripts": {
        "test": "(cd ./api && npm start) & (cd ./client && npm install && npm run build)",
        "start": "(cd ./api && npm start) & (cd ./client && npm start)"
    },
    "devDependencies": {
        "react-docgen-markdown-renderer": "^2.1.3"
    }
}
